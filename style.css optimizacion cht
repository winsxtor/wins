/* === OPTIMIZED CSS === */

/* General body styles */
body {
  font-family: 'Roboto Mono', monospace;
  margin: 0;
  padding: 0;
  background-color: #000;
  color: #fff;
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
  line-height: 1.6;
  text-align: justify;
}

/* Header */
header {
  background: rgba(0, 0, 0, 1);
  padding: 20px;
  color: white;
  text-align: center;
}

.logo-link {
  display: inline-block;
}

.logo {
  max-width: 200px;
  width: 100%;
  height: auto;
}

header h1 {
  font-size: 2.5em;
  margin: 0;
  color: #fff;
}

/* Navigation */
nav ul {
  list-style: none;
  padding: 0;
  margin-top: 15px;
  text-align: center;
}

nav ul li {
  display: inline;
  margin: 0 15px;
}

nav a {
  color: #fff;
  text-decoration: none;
  font-size: 1.1em;
  font-weight: bold;
}

/* Hero */
.hero {
  padding: 60px;
  text-align: center;
  background-color: rgba(0, 0, 0, 0.5);
}

/* Events */
.events {
  padding: 40px 20px;
  font-family: 'Public Sans', sans-serif;
  font-size: 13.5px;
  line-height: 1.8;
  background-color: rgba(255, 140, 0, 0.6);
  color: #fff;
  text-align: justify;
}

.events h3,
#soundcloud h3,
#galeria h3,
#trayectoria h3,
#tour h3 {
  font-size: 1.7em;
  margin-bottom: 20px;
  text-align: center;
}

.events ul,
#trayectoria ul,
#tour ul {
  list-style: disc;
  padding-left: 20px;
}

.events ul li,
#trayectoria ul li,
#tour ul li {
  font-size: 1.2em;
  margin-bottom: 15px;
  line-height: 1.8;
  border-bottom: 1px solid #fff;
  padding-bottom: 10px;
}

#tour ul li {
  border-color: #000;
}

/* SoundCloud */
.soundcloud-player {
  padding: 20px;
  text-align: center;
  background-color: rgba(255, 140, 0, 0.6);
  margin: 20px 0;
  border-radius: 8px;
}

.soundcloud-player iframe {
  border-radius: 8px;
  width: 100%;
  max-width: 800px;
  height: 166px;
}

/* Contact */
#contacto,
#about,
#youtube {
  padding: 50px;
  background-color: #000;
  background-image: url('FONDOWEB1.png');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  color: #fff;
  text-align: center;
}

#contacto p,
#about p {
  text-align: justify;
  color: #ccc;
}

form {
  background-color: #000;
  padding: 30px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
  width: 50%;
  margin: 0 auto;
  border: 1.7px solid rgba(169, 169, 169, 0.6);
}

form label {
  display: block;
  font-size: 1.1em;
  margin-bottom: 10px;
  color: #fff;
}

form input,
form textarea {
  width: 100%;
  padding: 12px;
  margin-bottom: 20px;
  border: 1px solid #444;
  border-radius: 6px;
  background: #fff;
  color: #222;
  font-size: 1em;
}

form button {
  background-color: #000;
  color: white;
  padding: 12px 25px;
  font-size: 1.2em;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  width: 100%;
  margin-top: 10px;
  transition: background-color 0.3s ease;
}

form button:hover {
  background-color: #3e1e6a;
}

form h2 {
  color: #552582;
  font-size: 2em;
  margin-bottom: 10px;
}

form p {
  color: #333;
  font-size: 1.1em;
  text-align: center;
}

/* Gallery */
.galeria-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
  margin-top: 20px;
}

.foto img {
  width: 100%;
  height: auto;
  cursor: pointer;
  border-radius: 8px;
  transition: transform 0.3s ease-in-out;
}

.foto img:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

.foto img:focus {
  outline: none;
  border: none;
}

/* Popup */
.popup {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.8);
  justify-content: center;
  align-items: center;
}

.popup img {
  max-width: 90%;
  max-height: 90%;
  border-radius: 8px;
}

.popup:target {
  display: flex;
}

/* Social Media */
#social-media {
  padding: 40px 80px;
  background-color: rgba(255, 255, 255, 0.9);
  color: #000;
  text-align: center;
  margin-top: 40px;
  border-radius: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.social-buttons {
  display: flex;
  justify-content: center;
  gap: 15px;
  flex-wrap: wrap;
  margin-top: 20px;
}

.social-btn {
  display: inline-flex;
  justify-content: center;
  align-items: center;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background-color: #fff;
  border: 2px solid transparent;
  transition: 0.3s ease;
}

.social-btn i {
  font-size: 24px;
  color: inherit;
}

.instagram { color: #E4405F; }
.facebook { color: #3b5998; }
.tiktok { color: #000; }
soundcloud { color: #FF5500; }

.social-btn:hover {
  opacity: 0.8;
  border: 2px solid #fff;
}

.social-btn:hover.instagram { border-color: #C13584; }
.social-btn:hover.facebook { border-color: #3b5998; }
.social-btn:hover.tiktok { border-color: #39b54a; }
.social-btn:hover.soundcloud { border-color: #ff2a00; }

/* YouTube */
#youtube img {
  width: 100%;
  max-width: 800px;
  border-radius: 8px;
  cursor: pointer;
}

.logo-video {
  width: 100%;
  height: auto;
}

/* Footer */
footer {
  background-color: #000;
  color: #fff;
  text-align: center;
  padding: 20px 0;
  position: relative;
  bottom: 0;
  width: 100%;
}

footer ul {
  list-style: none;
  padding: 0;
  margin-top: 10px;
  display: flex;
  justify-content: center;
}

footer ul li {
  margin: 0 15px;
}

footer ul li a {
  color: #fff;
  text-decoration: none;
}

footer ul li a:hover {
  text-decoration: underline;
}

/* Responsive */
@media (max-width: 768px) {
  header h1,
  #trayectoria h3,
  #tour h3 {
    font-size: 1.5em;
  }

  nav ul li,
  .container,
  form,
  .about-container {
    width: 90%;
    padding: 15px;
  }

  .logo {
    width: 40%;
    max-width: 150px;
  }

  .galeria-grid {
    grid-template-columns: 1fr;
  }

  #youtube img {
    max-width: 100%;
  }
}

/* Container reuse */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.about-container {
  background-color: #fff;
  color: #000;
  border-radius: 8px;
  padding: 20px;
  margin: 0 auto;
  max-width: 800px;
  text-align: center;
}

.about-container h2 {
  font-size: 1.7em;
  margin-bottom: 20px;
}
